# 2026-04-22

## Systemic PATH bug: claude not found in cron/systemd

`claude` binary lives at `/home/joe/.local/bin/claude`, which is not in default PATH for either systemd services or cron jobs. This caused silent failures across multiple tools.

**Fixed today:**
- Crontab: added `PATH=/home/joe/.local/bin:/usr/local/bin:/usr/bin:/bin` at the top
- `bodkin-daily-log.service`: added `Environment=PATH=/home/joe/.local/bin:/usr/local/bin:/usr/bin:/bin` + `systemctl daemon-reload`

**Still broken as of today:** `vault-gardening-auto.sh` calls bare `claude` — same fix needed (either update the shebang/PATH in the script or add Environment= to any service that runs it). Daily log entries for Apr 20 and Apr 21 were never written.

## Memory discipline improvements

Two changes made to harden cross-session memory:
1. **CLAUDE.md** — added explicit `Memory discipline` section with concrete triggers per memory type
2. **Stop hook** in `settings.json` — fires after every turn with a one-line reminder to scan for saveable memories. Uses `decision: block` so it actually appears in context. Note: hook fired 80+ times in one session (fires on every stop, not just end of conversation) — this is noisy but working as intended.

Task files created in `~/agent/tasks/`:
- `beets-music-pass.md` — rsync from MacBook is complete, beets pass is now unblocked
- `slskd-port-forward.md` — router port forward for slskd (needs Joe to execute router UI step)
- `reboot-persistence-test.md` — cold boot persistence check (needs Joe present)
- `weekly-task-triage.md` — recurring, runs independently

## GitHub hygiene audit

No GitHub MCP tools available — used the GitHub REST API via WebFetch. Hit an auth wall without a PAT: only the public `node-and-signal` repo was accessible (clean — no open PRs, stale branches, or issues). Private repos (including `claude-knowledge-base`) returned 401.

To make future audits useful: set `GITHUB_PERSONAL_ACCESS_TOKEN` in the agent environment with `repo` and `read:org` scopes.

## Beets music pass

Rsync from MacBook is confirmed complete as of 2026-04-22. The beets task file in `~/agent/tasks/` is ready to run — Bodkin can execute this autonomously by confirming rsync isn't still running and then calling beets.
